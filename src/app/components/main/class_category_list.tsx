export default function ClassCategoryList({activeCategory, setActiveCategory}: any) {
  return (
    <>
      <li className={
          activeCategory === 1 ? 'active' : ''
        } onClick={() => setActiveCategory(1)}>제작마케팅센터 지식나눔이 두줄이 되어버린다면!</li>
      <li className={
        activeCategory === 2 ? 'active' : ''
      } onClick={() => setActiveCategory(2)}>신입사원 필수 시청 영상</li>
      <li className={
        activeCategory === 2 ? 'active' : ''
      } onClick={() => setActiveCategory(2)}>신입사원 필수 시청 영상</li>
      <li className={
        activeCategory === 2 ? 'active' : ''
      } onClick={() => setActiveCategory(2)}>신입사원 필수 시청 영상</li>
      <li className={
        activeCategory === 2 ? 'active' : ''
      } onClick={() => setActiveCategory(2)}>신입사원 필수 시청 영상</li>
      <li className={
        activeCategory === 2 ? 'active' : ''
      } onClick={() => setActiveCategory(2)}>신입사원 필수 시청 영상</li>
      <li className={
        activeCategory === 2 ? 'active' : ''
      } onClick={() => setActiveCategory(2)}>신입사원 필수 시청 영상</li>
      <li className={
        activeCategory === 2 ? 'active' : ''
      } onClick={() => setActiveCategory(2)}>신입사원 필수 시청 영상</li>
      <li className={
        activeCategory === 2 ? 'active' : ''
      } onClick={() => setActiveCategory(2)}>신입사원 필수 시청 영상</li>
      <li className={
        activeCategory === 2 ? 'active' : ''
      } onClick={() => setActiveCategory(2)}>신입사원 필수 시청 영상</li>
      <li className={
        activeCategory === 2 ? 'active' : ''
      } onClick={() => setActiveCategory(2)}>신입사원 필수 시청 영상</li>
      <li className={
        activeCategory === 2 ? 'active' : ''
      } onClick={() => setActiveCategory(2)}>신입사원 필수 시청 영상</li>
      <li className={
        activeCategory === 2 ? 'active' : ''
      } onClick={() => setActiveCategory(2)}>신입사원 필수 시청 영상</li>
      <li className={
        activeCategory === 2 ? 'active' : ''
      } onClick={() => setActiveCategory(2)}>신입사원 필수 시청 영상</li>
      <li className={
        activeCategory === 2 ? 'active' : ''
      } onClick={() => setActiveCategory(2)}>신입사원 필수 시청 영상</li>
      <li className={
        activeCategory === 2 ? 'active' : ''
      } onClick={() => setActiveCategory(2)}>신입사원 필수 시청 영상</li>
    </>
  )
}